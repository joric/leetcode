name: Test Schedule
on:
  schedule:
    - cron: '*/5 * * * *' # every 5 minutes (UTC)
  workflow_dispatch:

jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - run: echo "scheduled ok $(date -u)"
